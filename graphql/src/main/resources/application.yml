spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=problemz
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
